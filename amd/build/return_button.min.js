/**
 * Return button (FAB) visibility and drag-to-reposition behaviour.
 *
 * Shows the floating action button only in the main window (hidden inside
 * iframes such as H5P activities) and lets the user drag it to a different
 * spot when it overlaps other UI. The chosen position is remembered in
 * sessionStorage, so it persists for the current browser session only.
 *
 * @module     local_dimensions/return_button
 * @copyright  2026 Anderson Blaine
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_dimensions/return_button",[],(function(){"use strict";var FAB_ID="local-dimensions-return-fab",STORAGE_KEY="local_dimensions_fab_pos",DRAG_THRESHOLD=5,EDGE_MARGIN=8;function readSavedPosition(){try{var raw=window.sessionStorage.getItem(STORAGE_KEY);if(!raw){return null}var pos=JSON.parse(raw);if(pos&&typeof pos.left==="number"&&typeof pos.top==="number"){return pos}}catch(e){}return null}function savePosition(left,top){try{window.sessionStorage.setItem(STORAGE_KEY,JSON.stringify({left:left,top:top}))}catch(e){}}function clearPosition(){try{window.sessionStorage.removeItem(STORAGE_KEY)}catch(e){}}function clamp(value,min,max){return Math.max(min,Math.min(value,max))}function applyPosition(fab,left,top){var maxLeft=window.innerWidth-fab.offsetWidth-EDGE_MARGIN;var maxTop=window.innerHeight-fab.offsetHeight-EDGE_MARGIN;left=clamp(left,EDGE_MARGIN,Math.max(EDGE_MARGIN,maxLeft));top=clamp(top,EDGE_MARGIN,Math.max(EDGE_MARGIN,maxTop));fab.style.left=left+"px";fab.style.top=top+"px";fab.style.right="auto";fab.style.bottom="auto"}function resetPosition(fab){clearPosition();fab.style.left="";fab.style.top="";fab.style.right="";fab.style.bottom=""}function enableDragging(fab){var dragging=false,moved=false,startX=0,startY=0,originLeft=0,originTop=0;fab.addEventListener("pointerdown",(function(e){if(e.pointerType==="mouse"&&e.button!==0){return}dragging=true;moved=false;startX=e.clientX;startY=e.clientY;var rect=fab.getBoundingClientRect();originLeft=rect.left;originTop=rect.top;fab.setPointerCapture(e.pointerId)}));fab.addEventListener("pointermove",(function(e){if(!dragging){return}var dx=e.clientX-startX;var dy=e.clientY-startY;if(!moved&&Math.abs(dx)<DRAG_THRESHOLD&&Math.abs(dy)<DRAG_THRESHOLD){return}moved=true;fab.classList.add("local-dimensions-fab-dragging");applyPosition(fab,originLeft+dx,originTop+dy);e.preventDefault()}));var endDrag=function(e){if(!dragging){return}dragging=false;fab.classList.remove("local-dimensions-fab-dragging");if(fab.hasPointerCapture&&fab.hasPointerCapture(e.pointerId)){fab.releasePointerCapture(e.pointerId)}if(moved){var rect=fab.getBoundingClientRect();savePosition(rect.left,rect.top)}};fab.addEventListener("pointerup",endDrag);fab.addEventListener("pointercancel",endDrag);fab.addEventListener("dragstart",(function(e){e.preventDefault()}));fab.addEventListener("click",(function(e){if(moved){e.preventDefault();e.stopPropagation();moved=false}}));fab.addEventListener("dblclick",(function(e){e.preventDefault();resetPosition(fab)}));window.addEventListener("resize",(function(){if(fab.style.left){var rect=fab.getBoundingClientRect();applyPosition(fab,rect.left,rect.top)}}))}return{init:function(){var fab=document.getElementById(FAB_ID);if(!fab||window!==window.parent){return}fab.style.display="flex";var saved=readSavedPosition();if(saved){applyPosition(fab,saved.left,saved.top)}enableDragging(fab)}}}));

//# sourceMappingURL=return_button.min.js.map
