/**
 * Chip filter manager.
 *
 * Wires up [data-chip-filters] containers rendered by the
 * local_dimensions/chip_filters Mustache partial. Tracks the active
 * selection per shortname (multi-select inside a group is OR; selections
 * across groups are AND) and invokes the host-page callback whenever the
 * selection changes.
 *
 * Each filterable item must carry a data-filtervalues attribute holding a
 * JSON object mapping shortname => value. Items are matched against the
 * active chip selection inside the host callback (or via the helper
 * `matchesSelection`).
 *
 * @module     local_dimensions/chip_filters
 * @copyright  2026 Anderson Blaine
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_dimensions/chip_filters",["local_dimensions/filter_tabs_nav"],(function(FilterTabsNav){var registry={};function readSelection(container){var selection={};return container.querySelectorAll('.local-dimensions-filter-tab[aria-pressed="true"]').forEach((function(chip){var group=chip.closest(".local-dimensions-filter-tabs-wrapper");if(group){var field=group.dataset.chipField,value=chip.dataset.chipValue;field&&(selection[field]||(selection[field]=[]),selection[field].push(value))}})),selection}function refreshClear(container,selection){var clearBtn=container.querySelector("[data-chip-clear]");if(clearBtn){var hasAny=Object.keys(selection).some((function(k){return selection[k]&&selection[k].length>0}));clearBtn.hidden=!hasAny}}return{init:function(containerId,callback){var container=document.getElementById(containerId);container&&(registry[containerId]={container:container,callback:callback},function(container,callback){container.querySelectorAll(".local-dimensions-filter-tab").forEach((function(chip){chip.addEventListener("click",(function(){var pressed="true"===chip.getAttribute("aria-pressed");chip.setAttribute("aria-pressed",pressed?"false":"true");var selection=readSelection(container);refreshClear(container,selection),FilterTabsNav.updateAll(container),"function"==typeof callback&&callback(selection)}))}));var clearBtn=container.querySelector("[data-chip-clear]");clearBtn&&clearBtn.addEventListener("click",(function(){!function(container){container.querySelectorAll('.local-dimensions-filter-tab[aria-pressed="true"]').forEach((function(chip){chip.setAttribute("aria-pressed","false")}))}(container);var selection=readSelection(container);refreshClear(container,selection),FilterTabsNav.updateAll(container),"function"==typeof callback&&callback(selection)}))}(container,callback),FilterTabsNav.initAll(container))},matchesSelection:function(selection,itemValues){for(var keys=Object.keys(selection||{}),i=0;i<keys.length;i++){var field=keys[i],allowed=selection[field];if(allowed&&0!==allowed.length){var actual=itemValues&&itemValues[field]||"";if(-1===allowed.indexOf(actual))return!1}}return!0}}}));

//# sourceMappingURL=chip_filters.min.js.map