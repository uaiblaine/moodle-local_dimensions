define("local_dimensions/central/course_datasource",["exports","core/ajax"],(function(_exports,_ajax){var obj;
/**
   * core/form-autocomplete datasource for the Competency hub add-course picker.
   *
   * Searches courses the user may link via local_dimensions_search_linkable_courses (competency read from
   * the select's data-competencyid; the server matches name, short name and ID number and excludes hidden
   * courses) and drops any course id in the select's data-exclude attribute (the courses already linked).
   * data-exclude is read fresh from the DOM on each search, so it stays correct after the linked list
   * changes. Suggestions show the course name plus the short name in monospace.
   *
   * @module     local_dimensions/central/course_datasource
   * @copyright  2026 Anderson Blaine
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.transport=_exports.processResults=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};_exports.transport=(selector,query,success,failure)=>{const source=document.querySelector(selector),competencyid=source&&source.dataset.competencyid?Number(source.dataset.competencyid):0;_ajax.default.call([{methodname:"local_dimensions_search_linkable_courses",args:{competencyid:competencyid,query:query,limitfrom:0,limitnum:50}}])[0].then((response=>success(response.items))).catch(failure)};const escapeHtml=text=>{const div=document.createElement("div");return div.textContent=text,div.innerHTML};_exports.processResults=(selector,results)=>{const source=document.querySelector(selector),raw=source&&source.dataset.exclude?source.dataset.exclude:"",excluded=new Set(raw.split(",").filter((id=>""!==id)));return results.filter((course=>!excluded.has(String(course.id)))).map((course=>{let label=escapeHtml(course.fullname);return course.shortname&&(label+=' <span class="font-monospace small local-dimensions-central-links-code">'+"".concat(escapeHtml(course.shortname),"</span>")),{value:course.id,label:label}}))}}));

//# sourceMappingURL=course_datasource.min.js.map