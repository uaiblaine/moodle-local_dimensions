define("local_dimensions/central/cohort_datasource",["exports","core/ajax"],(function(_exports,_ajax){var obj;
/**
   * core/form-autocomplete datasource for the Competency hub cohort picker.
   *
   * Searches cohorts via core_cohort_search_cohorts (context read from the select's data-contextid) and
   * drops any cohort whose id is in the select's data-exclude attribute (the cohorts already attached to
   * the template). data-exclude is read fresh from the DOM on each search, so it stays correct after the
   * attached list changes — unlike core/form-cohort-selector, which caches it via jQuery .data().
   *
   * @module     local_dimensions/central/cohort_datasource
   * @copyright  2026 Anderson Blaine
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.transport=_exports.processResults=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};_exports.transport=(selector,query,success,failure)=>{const source=document.querySelector(selector),contextid=source&&source.dataset.contextid?Number(source.dataset.contextid):0;_ajax.default.call([{methodname:"core_cohort_search_cohorts",args:{query:query,context:{contextid:contextid},limitfrom:0,limitnum:25}}])[0].then((response=>success(response.cohorts))).catch(failure)};_exports.processResults=(selector,results)=>{const source=document.querySelector(selector),raw=source&&source.dataset.exclude?source.dataset.exclude:"",excluded=new Set(raw.split(",").filter((id=>""!==id)));return results.filter((cohort=>!excluded.has(String(cohort.id)))).map((cohort=>({value:cohort.id,label:cohort.name})))}}));

//# sourceMappingURL=cohort_datasource.min.js.map