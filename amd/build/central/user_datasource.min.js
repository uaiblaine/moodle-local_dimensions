define("local_dimensions/central/user_datasource",["exports","core/ajax","core/str"],(function(_exports,_ajax,_str){var obj;
/**
   * core/form-autocomplete datasource for the Competency hub participant (user) picker.
   *
   * Searches via local_dimensions_search_assignable_users (template read from the select's
   * data-templateid), so users who already have a plan created from the template — individually
   * or by cohort sync — are excluded server-side: core refuses a second plan from the same
   * template, so suggesting them only misleads. Identity fields render in monospace next to
   * the name when the server exposes them.
   *
   * @module     local_dimensions/central/user_datasource
   * @copyright  2026 Anderson Blaine
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.transport=_exports.processResults=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};_exports.transport=async(selector,query,success,failure)=>{const source=document.querySelector(selector),templateid=source&&source.dataset.templateid?Number(source.dataset.templateid):0;try{const response=await _ajax.default.call([{methodname:"local_dimensions_search_assignable_users",args:{templateid:templateid,query:query,limitfrom:0,limitnum:25}}])[0];if(response.total>response.items.length)return void success(await(0,_str.getString)("search_toomany","local_dimensions"));success(response.items)}catch(error){failure(error)}};const escapeHtml=text=>{const div=document.createElement("div");return div.textContent=text,div.innerHTML};_exports.processResults=(selector,results)=>Array.isArray(results)?results.map((user=>{let label=escapeHtml(user.fullname);return user.identity&&(label+=' <span class="font-monospace small local-dimensions-central-links-code">'+"".concat(escapeHtml(user.identity),"</span>")),{value:user.id,label:label}})):results}));

//# sourceMappingURL=user_datasource.min.js.map