define("local_dimensions/central/category_datasource",["exports","core/ajax"],(function(_exports,_ajax){var obj;
/**
   * core/form-autocomplete datasource for the Competency hub's course category picker.
   *
   * Searches categories through local_dimensions_search_categories as the viewer types, so the
   * page never renders every category of the site. The "show hidden categories" toggle of the
   * bar is read fresh on each search, and each hit's counts are remembered so the headline
   * counter can follow a selection without another round-trip.
   *
   * @module     local_dimensions/central/category_datasource
   * @copyright  2026 Anderson Blaine
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.transport=_exports.processResults=_exports.known=void 0,_ajax=(obj=_ajax)&&obj.__esModule?obj:{default:obj};const SELECTORS_bar='[data-region="contextbar"]',SELECTORS_hiddenCatsToggle='[data-action="toggle-hidden-cats"]',seen=new Map,barOf=selector=>{const source=document.querySelector(selector);return source?source.closest(SELECTORS_bar):null};_exports.transport=(selector,query,success,failure)=>{const bar=barOf(selector),toggle=bar?bar.querySelector(SELECTORS_hiddenCatsToggle):null;_ajax.default.call([{methodname:"local_dimensions_search_categories",args:{query:query,includehidden:!(!toggle||!toggle.checked),limitnum:25}}])[0].then((response=>success(response.items))).catch(failure)};_exports.processResults=(selector,results)=>{const bar=barOf(selector),mode=bar&&"plans"===bar.dataset.activemode?"plans":"structure";return results.map((item=>{seen.set(String(item.id),{name:item.name,frameworks:Number(item.frameworkcount)||0,plans:Number(item.templatecount)||0,hidden:Boolean(item.hidden)});const count="plans"===mode?item.templatecount:item.frameworkcount;return{value:item.id,label:"".concat(item.name," (").concat(count,")")}}))};_exports.known=id=>seen.get(String(id))||null}));

//# sourceMappingURL=category_datasource.min.js.map