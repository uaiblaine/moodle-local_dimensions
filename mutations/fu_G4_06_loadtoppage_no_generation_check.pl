s/        \/\/ A mode, query or framework switch reset the list while this page was in flight: the page\n        \/\/ answers the old request, and the offset and total now belong to the new one\.\n        if \(generation !== state\.generation\) \{\n            return;\n        \}\n        appendNodes\(state, state\.listEl, response\.items, 0\);/        appendNodes(state, state.listEl, response.items, 0);/;
