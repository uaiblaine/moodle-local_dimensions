s/\(bool\) get_config\('local_dimensions', 'showrelatedlink'\)\n        \);\n    \}\n\n    \/\*\*\n     \* Resolve an lp inherit\/yes\/no toggle field to a bool, falling back to the global default\./(bool) get_config('local_dimensions', 'showrelated')\n        );\n    }\n\n    \/**\n     * Resolve an lp inherit\/yes\/no toggle field to a bool, falling back to the global default./;
