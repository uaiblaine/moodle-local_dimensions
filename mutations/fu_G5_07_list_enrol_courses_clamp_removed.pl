s/        \$limitfrom = max\(0, \$params\['limitfrom'\]\);\n        \$limitnum = \$params\['limitnum'\] > 0 \? min\(\$params\['limitnum'\], self::MAX_LIMIT\) : self::DEFAULT_LIMIT;\n        \$courses = array_slice\(\$courses, \$limitfrom, \$limitnum\);/        \$courses = array_slice(\$courses, \$params['limitfrom'], min(\$params['limitnum'], self::MAX_LIMIT));/;
