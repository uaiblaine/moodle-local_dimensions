s/"SELECT u\.id, \$namefields \$identityselects/"SELECT u.id, u.email, u.idnumber, \$namefields/;
s/\$items = \[\];\n        foreach \(\$records as \$record\) \{\n            \$identity = \[\];\n            foreach \(\$identityfields as \$field\) \{\n                \$value = \(string\) \(\$record->\$field \?\? ''\);\n                if \(\$value !== ''\) \{\n                    \$identity\[\] = \$value;\n                \}\n            \}\n/\$items = [];\n        foreach (\$records as \$record) {\n            \$identity = array_filter([\$record->email, \$record->idnumber]);\n/;
