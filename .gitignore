# OS files
.DS_Store

# IDE / Editor
.vscode/
.claude/
.idea/

# Node.js (build tooling only - not part of the plugin)
node_modules/
package.json
package-lock.json

# Linter config
.stylelintrc.json

# Design/planning docs are versioned (design-kit, learner-kit, superpowers specs
# and plans) for collaboration, but kept OUT of the release zip via .gitattributes
# (export-ignore) — the plugin ships only what Moodle needs at runtime.
